/// Class that combines two dates
class DateRange {
  /// Construct the DateRange
  const DateRange({
    required this.start,
    required this.end,
  });

  /// Start of the range
  final DateTime start;

  /// End of the range
  final DateTime end;
}
